
 * Exec the defines.
 * Initialize variables from expressions (combine var resolving code).
 * See what the TODO-marked code works for.
 * Modulo operator.
 * Negation operator.
 * Implement the float64 operators for not-exact divisions.
 * Add operator to join strings.
 * Line numbers in the error messages.
